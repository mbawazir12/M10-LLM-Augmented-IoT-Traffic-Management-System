## tests/

Automated evaluation is handled by `evaluation/run_eval.py`, which runs 40 labelled test cases
against the live bridge and scores accuracy, structural validity, and response latency.
Run it with the bridge active: `python evaluation/run_eval.py`
