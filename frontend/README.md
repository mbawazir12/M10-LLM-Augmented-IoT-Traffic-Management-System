## frontend/

The browser dashboard for this project is served directly by the FastAPI backend.
The dashboard HTML and assets live at `backend/static/dashboard.html`.
Open `http://localhost:8000` after starting the bridge to access the live traffic control UI.
